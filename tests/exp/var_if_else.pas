program exemplo;
var
 nome1, nome2:string;
 num,cont:integer;
begin
    num := 1 + 2;
    nome1 := 'If';
    nome2 := 'Else';
    if num < 2 then
        Writeln(nome1)
    else
        Writeln(nome2);
end.
